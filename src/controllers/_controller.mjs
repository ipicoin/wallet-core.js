import { EventEmitter } from "node:events";

class BasicController extends EventEmitter {
	// ...to rewriting because i do not like that manner, here should be something else than classing
	// every controller is single one item, not group of items
	// skriptonus?
}

export default BasicController;
